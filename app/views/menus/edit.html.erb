<%= simple_form_for @menu do |f| %>
  <%= f.input :diet, collection: Recipe::DIET %>
  <%= f.input :number_of_people, as: :numeric %>
  <%= f.input :number_of_meals, as: :numeric %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>

<%= link_to "Créer un menu_recipe random", menu_menu_recipes_path(@menu, recipe_id: "random"), method: :post %>
